import type { BoardCell, Character, Placement, Zone } from '../game/types'
import { getCell } from '../game/rules'

interface BoardProps { board: BoardCell[]; rows: number; columns: number; zones: Zone[]; placements: Placement[]; characters: Character[]; selectedId: string | null; onCellClick: (cell: BoardCell) => void }
export function Board({ board, rows, columns, zones, placements, characters, onCellClick }: BoardProps) {
  const labels = Array.from({ length: rows }, (_, i) => i + 1)
  return <div className="board-wrap"><div className="board-frame"><div className="column-labels" style={{ '--columns': columns } as React.CSSProperties} aria-hidden="true"><span />{Array.from({ length: columns }, (_, i) => <span key={i}>{i + 1}</span>)}</div><div className="board-with-rows" style={{ '--rows': rows } as React.CSSProperties}><div className="row-labels" aria-hidden="true">{labels.map(label => <span key={label}>{label}</span>)}</div><div className="board" style={{ '--columns': columns } as React.CSSProperties}>{Array.from({ length: rows * columns }, (_, i) => { const cell = board[i]; const person = placements.find(p => p.position.row === cell.row && p.position.column === cell.column); const char = person && characters.find(c => c.id === person.characterId); const rowUsed = placements.some(p => p.position.row === cell.row); const columnUsed = placements.some(p => p.position.column === cell.column); const top = getCell(board, { row: cell.row - 1, column: cell.column }); const left = getCell(board, { row: cell.row, column: cell.column - 1 }); return <button key={`${cell.row}-${cell.column}`} className={`cell zone-${cell.zoneId} ${!cell.occupiable ? 'blocked' : ''} ${rowUsed ? 'row-used' : ''} ${columnUsed ? 'column-used' : ''} ${top && top.zoneId !== cell.zoneId ? 'wall-top' : ''} ${left && left.zoneId !== cell.zoneId ? 'wall-left' : ''}`} onClick={() => onCellClick(cell)} aria-label={`Fila ${cell.row}, columna ${cell.column}${cell.object ? `, ${cell.object.label}` : ''}`}><span className="object">{cell.object?.icon}</span>{char && <span className={`placed ${char.isVictim ? 'placed-victim' : ''}`}>{char.avatar}<i>{char.name.split(' ')[0]}</i></span>}</button> })}</div></div></div><div className="zone-legend">{zones.map(zone => <span key={zone.id}><b className={`dot zone-${zone.tone}`} />{zone.name}</span>)}</div></div>
}
