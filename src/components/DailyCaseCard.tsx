import { Link } from 'react-router-dom'
import { formatDifficultyStars } from '../game/difficulty'
import type { DifficultyRating } from '../game/types'
export function DailyCaseCard({ dateLabel, completed, difficulty, maxDifficulty }: { dateLabel: string; completed: boolean; difficulty?: DifficultyRating; maxDifficulty: DifficultyRating }) { return <article className="case-card daily-card"><div><p className="eyebrow">CASO DIARIO</p><h2>{dateLabel}</h2><p>Un nuevo expediente espera hoy.</p><span className="difficulty">{difficulty ? `✦ Dificultad: ${formatDifficultyStars(difficulty)}` : `Disponibles hasta ${formatDifficultyStars(maxDifficulty)}`}</span></div><div className="case-card-footer"><span className="available">{completed ? '✓ COMPLETADO HOY' : difficulty ? '● INICIADO' : '● DISPONIBLE'}</span>{completed ? <span>VUELVE MAÑANA</span> : <Link className="primary" to="/daily">{difficulty ? 'CONTINUAR CASO DIARIO' : 'JUGAR CASO DIARIO'} <span>→</span></Link>}</div></article> }
