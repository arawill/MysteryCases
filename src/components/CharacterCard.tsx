import type { Character } from '../game/types'
export function CharacterCard({ character, selected, placed, onSelect }: { character: Character; selected: boolean; placed: boolean; onSelect: () => void }) { return <button className={`character-card ${selected ? 'selected' : ''} ${character.isVictim ? 'victim' : ''}`} onClick={onSelect} aria-pressed={selected}><span className="avatar">{character.avatar}</span><span className="character-info"><strong>{character.name}</strong>{character.isVictim && <small>VÍCTIMA</small>}<em>{placed ? 'En escena' : 'Sin colocar'}</em>{character.clues.map(clue => <span className="clue" key={clue.id}>{clue.text}</span>)}</span></button> }
