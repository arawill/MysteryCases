# Copy this file to android/keystore.properties and replace each placeholder locally.
# Never commit android/keystore.properties or the keystore it references.
storeFile=keystore/mysterycases-release.jks
storePassword=CHANGE_ME
keyAlias=mysterycases
keyPassword=CHANGE_ME
